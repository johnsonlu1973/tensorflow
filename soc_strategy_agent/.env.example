# Required
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Perplexity is preferred for today-only news filtering.
# Without this key, Agent 1 falls back to Claude web_search (slower, more API calls).
PERPLEXITY_API_KEY=your_perplexity_api_key_here
